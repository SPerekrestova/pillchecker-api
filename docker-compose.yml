services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - model-cache:/app/models
      - ./data:/app/data
    environment:
      - HF_HOME=/app/models
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api
    restart: unless-stopped

volumes:
  model-cache:
