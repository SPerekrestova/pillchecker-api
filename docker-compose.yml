services:
  api:
    build:
      context: .
      target: runtime
    ports:
      - "8000:8000"
    volumes:
      - model-cache:/app/models
    environment:
      - HF_HOME=/app/models
      - HF_TOKEN=${HF_TOKEN:-}
      - API_KEY=${API_KEY}
    restart: unless-stopped

volumes:
  model-cache:
